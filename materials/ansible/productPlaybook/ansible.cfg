[defaults]
inventory = ./inventory/hosts
verbosity = 0
#remote_user = adminstd
private_key_file = ~/.ssh/id_ed25519
host_key_checking = False

[privilege_escalation]
#become = True
#become_method = sudo
#become_user = adminstd
